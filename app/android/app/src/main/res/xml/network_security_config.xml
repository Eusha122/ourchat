<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev-only: allow plain HTTP to the local backend LAN IP so a physical
         device can reach the dev server. Update the IP if it changes
         (see api_client.dart _devMachineLanIp). Remove before shipping. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.0.100</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
